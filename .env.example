# ═══════════════════════════════════════════════════════════════════════════════
# REQUIRED secrets — all must be set before running docker compose up.
# Variables marked REQUIRED will cause docker compose to refuse to start if unset.
# Generate random values with: openssl rand -hex 32
# ═══════════════════════════════════════════════════════════════════════════════

# ── MongoDB (REQUIRED) ────────────────────────────────────────────────────────
MONGO_USER=bugfinder
# REQUIRED — change from example value before deploying
MONGO_PASSWORD=CHANGE_ME_use_openssl_rand_hex_32
MONGO_DB=bugfinder
MONGODB_URI=mongodb://bugfinder:CHANGE_ME@localhost:27017/bugfinder?authSource=admin

# ── Node Environment ──────────────────────────────────────────────────────────
NODE_ENV=development
PORT=5000

# ── Session (REQUIRED) ───────────────────────────────────────────────────────
SESSION_SECRET=CHANGE_ME_use_openssl_rand_hex_32

# ── OpenCode / OpenAI ─────────────────────────────────────────────────────────
OPENCODE_API_KEY=your_opencode_api_key_here
OPENCODE_API_BASE=https://opencode.ai/zen/v1
OPENCODE_MODEL=nemotron-3-super-free

# AI token budget per user per day (estimated tokens, ~4 chars/token)
AI_DAILY_TOKEN_BUDGET=50000

# Data retention: audit logs TTL in days (default 365)
DATA_RETENTION_DAYS=365

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ── SMTP (email alerts & scheduled reports) ───────────────────────────────────
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM=noreply@bugfinder.io
ADMIN_EMAIL=admin@bugfinder.local

# ── SAML 2.0 SSO ─────────────────────────────────────────────────────────────
SAML_ENTRY_POINT=https://your-idp.com/saml/sso
SAML_CALLBACK_URL=https://your-app.com/api/auth/saml/callback
SAML_ISSUER=bugfinder
# Base64-encoded X.509 certificate from your IdP
SAML_IDP_CERT=
# PEM-encoded SP private key (optional, for signed AuthnRequests)
SAML_PRIVATE_KEY=

# ── WebAuthn / Passkeys ───────────────────────────────────────────────────────
WEBAUTHN_RP_NAME=Bug Finder
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:5173

# ── OAuth (GitHub / Google OIDC) ─────────────────────────────────────────────
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OIDC_CALLBACK_URL=http://localhost:5000/api/auth/oidc/callback

# ── Vault / Secrets Management ───────────────────────────────────────────────
# HashiCorp Vault (preferred)
VAULT_ADDR=https://vault.example.com
VAULT_TOKEN=
VAULT_SECRET_PATH=secret/data/bugfinder

# AWS Secrets Manager (fallback if Vault is unavailable)
AWS_REGION=us-east-1
AWS_SECRET_ID=bugfinder/production

# ── Multi-Tenant ─────────────────────────────────────────────────────────────
MULTI_TENANT=false

# ── Integrations ─────────────────────────────────────────────────────────────
GITHUB_TOKEN=your_github_token_here
SLACK_WEBHOOK_URL=your_slack_webhook_url_here
SLACK_DEFAULT_CHANNEL=#security-alerts
TEAMS_WEBHOOK_URL=
PAGERDUTY_ROUTING_KEY=
JIRA_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_PROJECT_KEY=

# ── Security keys (REQUIRED) ─────────────────────────────────────────────────
# AES-256 key for credential vault encryption (must be >= 32 chars)
CREDENTIAL_VAULT_KEY=CHANGE_ME_use_openssl_rand_hex_32
# HMAC key for webhook payload signing
WEBHOOK_SECRET_KEY=CHANGE_ME_use_openssl_rand_hex_32
# Admin account password (REQUIRED)
ADMIN_PASSWORD=CHANGE_ME_strong_password

# ── Scanner Infrastructure ────────────────────────────────────────────────────
ZAP_URL=http://localhost:8090
ZAP_API_KEY=CHANGE_ME_use_openssl_rand_hex_16
PLAYWRIGHT_URL=http://localhost:3005
INTERACTSH_URL=http://localhost:8194
# REQUIRED — must match across api-server and interactsh containers
INTERACTSH_TOKEN=CHANGE_ME_use_openssl_rand_hex_16
INTERACTSH_DOMAIN=oast.local

# ── Scan concurrency (optional) ───────────────────────────────────────────────
MAX_CONCURRENT_SCANS=5

# ── Frontend ─────────────────────────────────────────────────────────────────
VITE_API_URL=http://localhost:5000/api

# ── TLS / nginx (production) ─────────────────────────────────────────────────
TLS_CERT_PATH=/etc/ssl/certs/bugfinder.crt
TLS_KEY_PATH=/etc/ssl/private/bugfinder.key
DOMAIN=bugfinder.example.com
